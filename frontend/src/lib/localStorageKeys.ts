export const LOCAL_STORAGE_USER_ROLE_KEY = "hustwork_user_role";

/** Tutor profile id for this browser (after create) */
export const LOCAL_STORAGE_MY_TUTOR_ID_KEY = "hustwork_my_tutor_id";

/** `{ token, user }` from auth API — see `useAuth` / `getInitialState` */
export const LOCAL_STORAGE_AUTH_KEY = "hustwork_auth";
